@model ASPImageApplication.Models.Image

@{
    ViewData["Title"] = "Details";
}

<h2><b>Details</b> For Image</h2>
<br/>
<div class="row">
    <div class="col-md-6">
        <img src="@Url.Action("Display", new { id=User.Identity.Name, dbId=Model.ImageId })" class="img-rounded" alt="Model.FileName" id="_image" />
    </div>

<div class="col-md-6">
    <h4>@Model.Title</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Owner)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Owner)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Title)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Description)
        </dd>
    </dl>
</div>
<p>
    <a asp-action="Edit" class="btn btn-default" asp-route-id="@Model.ImageId">Edit</a> |
    <a asp-action="Index" class="btn btn-success">Back to List</a>
</p>
    </div>
