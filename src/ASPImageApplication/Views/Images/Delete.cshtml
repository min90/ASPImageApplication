@model ASPImageApplication.Models.Image

@{
    ViewData["Title"] = "Delete Image";
}

<h3>Are you sure you want to delete this?</h3>
<div>
<div class="row">
    <h4>Image @Model.Title</h4>
    <hr />
    <div class="col-md-1">
    <img src="@Url.Action("Display", new { id=User.Identity.Name, dbId=Model.ImageId })" class="img-rounded" alt="Model.FileName" id="deleted_image" />
    </div>
    <div class="col-md-11">
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Owner)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Owner)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Title)
        </dd>
    </dl>
    </div>
    </div>
    <br />
    <form asp-action="Delete">
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            <a asp-action="Index" class="btn btn-success">Back to List</a>
        </div>
    </form>
</div>
